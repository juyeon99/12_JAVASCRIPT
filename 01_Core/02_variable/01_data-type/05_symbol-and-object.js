// 이름이 충돌할 위험이 없는 유일한 값인 symbol을 property key로 사용

// symbol
obj[key] = 'value';
// console.log(obj[key]);

/* 
    객체 타입
    JS의 데이터 타입은 원시타입(number, string, boolean, undefined, null, symbol)
    이 이외의 모든 값은 객체 타입(객체, 함수, 배열...)
*/